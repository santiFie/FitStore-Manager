<p style="color: green"><%= notice %></p>

<% content_for :title, "Ventas" %>

<h1>Ventas</h1>

<div id="sales">
  <% @sales.each do |sale| %>
    <%= render sale %>
    <p>
      <%= link_to "Ver venta", sale %>
    </p>
  <% end %>
</div>

<%= link_to "Nueva venta", new_sale_path %>
